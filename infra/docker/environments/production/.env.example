# ============================================
# Yape Notifier - Production Environment Variables
# ============================================
# 
#  IMPORTANTE: Este archivo es SOLO DOCUMENTACIÓN
# 
# El archivo .env REAL debe existir previamente con los secretos configurados.
# El script deploy.sh NO copia este archivo. Debes crear .env manualmente.
#
# ============================================
# Variables para Docker Compose
# ============================================
# Estas variables se usan en docker-compose.yml para configurar los servicios

# Base de datos PostgreSQL
#  OBLIGATORIO: Debes configurar una contraseña segura
DB_PASSWORD=TU_CONTRASEÑA_SEGURA_AQUI

# Nombre de la base de datos (opcional, default: yape_notifier)
DB_DATABASE=yape_notifier

# Usuario de PostgreSQL (opcional, default: postgres)
DB_USERNAME=postgres

# URL de la API (opcional, default: https://api.notificaciones.space)
APP_URL=https://api.notificaciones.space

# URL de la API para el Dashboard (opcional, default: https://api.notificaciones.space)
DASHBOARD_API_URL=https://api.notificaciones.space

# ============================================
# Variables para Laravel (dentro del contenedor)
# ============================================
# Estas variables se pasan al contenedor PHP-FPM y Laravel las lee desde su .env interno
# Nota: Laravel tiene su propio archivo .env dentro del contenedor

# Clave de aplicación Laravel
#  IMPORTANTE: 
#   - Si está vacío (APP_KEY=), el script deploy.sh lo generará SOLO UNA VEZ en el primer despliegue
#   - Si ya tiene un valor, NUNCA se regenerará automáticamente
#   - Formato: base64:xxxxxxxxxxxxx
#   - Para generar manualmente: docker compose exec php-fpm php artisan key:generate
APP_KEY=

# ============================================
# Notas de Seguridad
# ============================================
# 
# 1. DB_PASSWORD: 
#    - DEBE ser una contraseña segura y única
#    - NO uses valores de ejemplo ni placeholders
#    - NO lo compartas ni lo subas a repositorios
#
# 2. APP_KEY:
#    - Se genera automáticamente en el primer despliegue si está vacío
#    - Una vez generado, NUNCA se regenera automáticamente
#    - Si regeneras APP_KEY en producción, los datos encriptados se perderán
#    - Mantén una copia de seguridad de APP_KEY
#
# 3. Archivo .env:
#    - El archivo .env REAL debe existir antes de ejecutar deploy.sh
#    - NO se copia desde .env.example automáticamente
#    - Debe estar en el mismo directorio que docker-compose.yml
#    - NO debe estar en el control de versiones (debe estar en .gitignore)
#
# ============================================
