# Example override file for local development
# Copy this to docker-compose.override.yml and adjust as needed

version: '3.8'

services:
  dashboard-dev:
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
    volumes:
      - .:/app
      - /app/node_modules

